import React from 'react'

import {Cpu} from '@styled-icons/remix-line/Cpu'

// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
export default (props: any) => <i><Cpu size="20" {...props}/></i>
