import React from 'react'

import {Check} from '@styled-icons/entypo/Check'

// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
export default (props: any) => <i><Check  size="22" {...props}/></i>
